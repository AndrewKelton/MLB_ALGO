#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "innings_instr.h"
#include "utilities.h"

#define MAX_TEAMS 30



int main(/*int argc, char * argv[]*/) 
{ 
    teams_list_t * teams_list = init_teams_list();

    if (!teams_list)
        

    // while (teams_list) {
    //     printf("%s --- %d\n", teams_list->team->team_name->key, teams_list->team->stats->rank);
    //     teams_list = teams_list->next;
    // }
        
    return 0;
}